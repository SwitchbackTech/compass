---
description: Compass styling conventions for web components and stylesheets
globs: packages/web/**/*.{tsx,css}
alwaysApply: false
---

# Web styles

See `AGENTS.md`'s Compass-Specific Rules for Tailwind semantic colors and
canonical-scale-utility conventions.

- Preserve the established Compass visual language and responsive behavior.
- Use native semantic elements and visible focus states for interactive UI.
- Never use em-dashes (`—`) in user-facing copy. Use a comma, a period, or a
  colon instead. Code comments are unaffected.

```tsx
// Prefer
<div className="bg-surface text-foreground" />

// Avoid
<div className="bg-blue-300 text-zinc-900" />
```
