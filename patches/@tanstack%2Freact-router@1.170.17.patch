diff --git a/dist/cjs/CatchBoundary.cjs b/dist/cjs/CatchBoundary.cjs
index 8ad0d9bfe97b2cfa8a8ebafb85e02dea54bb5866..a6ec90c9a9ae90bbef7a3026c12fb3e06016f181 100644
--- a/dist/cjs/CatchBoundary.cjs
+++ b/dist/cjs/CatchBoundary.cjs
@@ -32,7 +32,7 @@ var CatchBoundaryImpl = class extends react.Component {
 		return { resetKey };
 	}
 	static getDerivedStateFromError(error) {
-		return { error };
+		return { error: error || new Error(`Unhandled falsy error: ${String(error)}`) };
 	}
 	reset() {
 		this.setState({ error: null });
diff --git a/dist/cjs/Match.cjs b/dist/cjs/Match.cjs
index d6347522e1d49691a91d5df3869351519ac4ae10..c44b9b2fe41673e2f987d40a57723b773a3a08dc 100644
--- a/dist/cjs/Match.cjs
+++ b/dist/cjs/Match.cjs
@@ -127,7 +127,11 @@ function OnRendered() {
 var MatchInner = react.memo(function MatchInnerImpl({ matchId }) {
 	const router = require_useRouter.useRouter();
 	const getMatchPromise = (match, key) => {
-		return router.getMatch(match.id)?._nonReactive[key] ?? match._nonReactive[key];
+		// Fallback never-resolving promise: during cold beforeLoad redirects the
+		// match can be torn down while still pending/redirected, leaving
+		// loadPromise undefined. Throwing undefined escapes Suspense and crashes
+		// the app (TanStack/router#7457). See also open upstream PR #7741.
+		return router.getMatch(match.id)?._nonReactive[key] ?? match._nonReactive[key] ?? new Promise(() => {});
 	};
 	if (_tanstack_router_core_isServer.isServer ?? router.isServer) {
 		const match = router.stores.matchStores.get(matchId)?.get();
diff --git a/dist/esm/CatchBoundary.js b/dist/esm/CatchBoundary.js
index 1f47185042beff96ba6c47852df41cb9fd406ea2..cc9c4c8acdabad55fbee9c32ce0c21ce78bc0e46 100644
--- a/dist/esm/CatchBoundary.js
+++ b/dist/esm/CatchBoundary.js
@@ -30,7 +30,7 @@ var CatchBoundaryImpl = class extends React$1.Component {
 		return { resetKey };
 	}
 	static getDerivedStateFromError(error) {
-		return { error };
+		return { error: error || new Error(`Unhandled falsy error: ${String(error)}`) };
 	}
 	reset() {
 		this.setState({ error: null });
diff --git a/dist/esm/Match.js b/dist/esm/Match.js
index 8da8d0f108b2dcd57fc6c9a853317d164e31776b..a9a5c058b1c1f921cfeb9ef6fa4e89310582d234 100644
--- a/dist/esm/Match.js
+++ b/dist/esm/Match.js
@@ -125,7 +125,11 @@ function OnRendered() {
 var MatchInner = React$1.memo(function MatchInnerImpl({ matchId }) {
 	const router = useRouter();
 	const getMatchPromise = (match, key) => {
-		return router.getMatch(match.id)?._nonReactive[key] ?? match._nonReactive[key];
+		// Fallback never-resolving promise: during cold beforeLoad redirects the
+		// match can be torn down while still pending/redirected, leaving
+		// loadPromise undefined. Throwing undefined escapes Suspense and crashes
+		// the app (TanStack/router#7457). See also open upstream PR #7741.
+		return router.getMatch(match.id)?._nonReactive[key] ?? match._nonReactive[key] ?? new Promise(() => {});
 	};
 	if (isServer ?? router.isServer) {
 		const match = router.stores.matchStores.get(matchId)?.get();
