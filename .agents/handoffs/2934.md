---
schema_version: 1
task_id: "2934"
from: Reviewer
to: Manager
owner: Manager
status: verifying
artifact:
  - path: packages/web/src/billing/BillingGateModal.tsx
  - path: packages/web/src/billing/billing-preview.store.ts
  - path: packages/web/src/billing/BillingReadOnlyBanner.tsx
  - url: https://github.com/KeepSoftwareSimple/compass-calendar/pull/2934
evidence:
  - command: bun run verify
    result: "Selected packages: web, backend. Checks run: test:web, test:backend, type-check, lint, knip, test:a11y all green. test:e2e 46 passed / 1 failed."
  - command: bun run test:e2e timed
    result: "e2e/timed/mouse-inert.spec.ts:62 also fails on origin/main in a clean detached worktree, so the single e2e failure is pre-existing and not from this diff."
  - command: bun test:e2e onboarding
    result: "6 passed"
  - command: /simplify
    result: "4 cleanup agents; 6 findings applied in 4ed72dbd1 (banner dedupe, shared redirect with error handling, single RootShell tree, preview-status invariant + regression test, gate-overlay inlined, unreachable guard collapsed); store-ceremony and lint-check findings skipped."
  - command: /code-review high
    result: "3 findings. High: the simplify pass removed a load-bearing configQuery.isError guard (TanStack retains stale data on a failed refetch), breaking the documented fail-open. Fixed in f5b8349dc. Two low findings accepted as-is."
assumptions:
  - "Anonymous visitors should never be metered; a trial is asked for only at sign up / sign in / connect account. Confirmed with the founder before implementation."
  - "The read-only look-around is safe because assertBillingAllowsWrites still 403s every write regardless of client state."
open_risks:
  - "Preview flag is not cleared when a status change revokes the look-around; a status round-trip could re-show the banner. In-memory, self-corrects on reload."
  - "useBillingRedirect's in-flight latch reads a render closure, so a same-frame double click could open two Checkout sessions. The disabled prop closes it on the next commit."
  - "The gated path (awaiting_checkout) cannot be exercised locally without Stripe config. Covered by RootShell tests; real Checkout redirect still needs a staging pass."
  - "Orphaned compass.trial.started-at* keys remain in existing users' localStorage. Harmless, no migration written."
next_deadline: 2026-08-28T02:00:00Z
retry: 0
approval: none
waiting_on: null
escalation: null
---

Anonymous trial clock deleted; billing gate softened with an in-memory
read-only preview that a refused write reverses. Em-dash sweep plus the
convention recorded in AGENTS.md and .cursor/rules/web-styles.mdc.
