# Handoff: #3242

task_id: 3242
status: complete
branch: cursor/microsoft-recurrence-3242

## Summary

Implemented Microsoft Graph `patternedRecurrence` ↔ Compass RRULE conversion in
`packages/sync/src/providers/microsoft/` with CLDR-backed Windows/IANA zone
mapping.

## Deliverables

- `microsoft-recurrence.ts`: `toRRule`, `fromRRule`, `UnsupportedRecurrenceError`
- `windows-zones.ts`: generated CLDR `windowsZones` table (139 zones)
- `packages/scripts/src/commands/generate-windows-zones.ts`: regeneration script
- Tests: 26 Graph round-trips, 20 RRULE idempotence cases, zone validation

## Verify

```
bun test:sync
bun run type-check
bun lint
bun knip
```

## Notes

- Deprecated CLDR IANA aliases are canonicalized to `Intl`-supported names in
  the generator (e.g. `Asia/Calcutta` → `Asia/Kolkata`).
- Reverse RRULE conversion rejects `BYHOUR`, `BYSETPOS`, multiple `BYMONTH`, and
  mixed BYDAY ordinals with `unsupportedRecurrence`.
