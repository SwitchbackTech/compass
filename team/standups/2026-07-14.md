# Standup — 2026-07-14

**Window:** 2026-07-13 (`c1a5e344`) .. 2026-07-14 (`50a14691f`)
**Main / CI:** Green. 20 commits merged, 258 files, +3526 / -3872 (net deletion).
Test 1m21s–1m46s wall, Release ~3m30s, CodeQL ~2m. No open PRs.

**Headline:** Google sub-calendar v1 is built and validated on staging, but
**production is still on the legacy collection**. Four of six staging evidence
cells are now filled; the two `pending` cells (12-step manual acceptance,
rollback + second forward run) are the entire remaining distance between "built"
and "users have multi-calendar." Both are PO-gated — agents must not perform them.

## QA

### Recent work
- No net loss of live-behavior test coverage in this window's deletion-heavy commits: `9aab0eec1` (remove eventemitter) and `0c16d49ef` (unify command palette actions) deleted tests only alongside their corresponding dead source files (`pointer-position.tsx`, `useMovementEvent.ts`, `event-emitter.util.ts`, `event-target-visibility.util.ts`, `useWeekCmdTasks.ts`, `dayCalendarFocus.util.ts`); `be412e851` explicitly consolidated `event.validator.test.ts` cases into `compass-event.contracts.test.ts` rather than dropping coverage.
- Confirmed the 5 unlinked-looking commits (`50a14691f` datepicker contrast, `bac798783` shifted week window, `30397d745` datepicker colors, `3ffa55dcb` all-day row surfaces, `60e0c737f` hide active view shortcut) all ran `Test`/`Release on main`/`CodeQL` on push and passed — they went through CI, just without a PR number in the merge title.
- CI step-duration check: no single step in the `Test` job (lint, type-check, or any of the 4 unit-test matrix jobs) exceeds 46s (`unit (backend)` is the longest); the 1m21s–1m46s reported by `gh run list` is total parallel-job wall time, not a single step — charter's "no CI step over 1 minute" is currently satisfied, nothing to attack.

### Top 3 priorities
1. Nothing this window — reviewed the last 20 commits' test-deletion risk and CI step timings and found no gaps or violations against charter thresholds.
2. Run `qa-test-staging` sweep over this window's user-facing changes (datepicker contrast/colors, all-day row alignment, week-window-on-today preservation, command palette calendar-sync status) — new/changed interactive surfaces haven't had a real-browser QA pass yet.
3. Staging/prod log review for the sync/webhook hardening batch, noted as resolved-access in 2026-07-13 founder decisions — I did not have log access in this read-only session to actually perform the review; carry it forward as the next actionable item (not blocked, just not yet done).

### Blockers
- None — could not verify staging/prod log access first-hand this session (read-only exploration, no log tooling invoked), but per 2026-07-13 founder decisions access is already provisioned, so this is a to-do, not a blocker.

## Fullstack

### Recent work
- Sidebar event-details docking landed and stabilized: yesterday's `feat(web): dock event forms in the sidebar` (#2092) merged, followed by its planned decoupling follow-up `refactor(web): decouple sidebar details state` (#2106) — my D1–D7 decision log from 2026-07-13 is fully executed, no open items left in that note.
- Google sync got a three-fix hardening streak (unsupported watches, batched recurring instances, batched event imports — #2109 and two follow-ons); verified batching is consistent between the import and recurring-instance paths, no leftover TODOs or error-handling gaps in the diffs.
- Event-contract cleanup (#2113, #2116: LegacyEvent → CompassEvent) is fully carried through — no dead types or half-migrated call sites; the remaining `Legacy*` file names in `packages/web/src/common/storage/migrations` and `packages/scripts/src/common/legacy-event.transform.ts` are intentionally-named historical migration shims, not stragglers.

### Top 3 priorities
1. Deduplicate the hard-coded `perPage = 1000` default that's now independently repeated in 6 call sites across `packages/backend/src/event/google-event-sync.service.ts:109`, `packages/backend/src/sync/services/google-sync/google-sync.service.ts:104`, and `packages/backend/src/sync/services/import/google-import.service.ts:82,169,206` — pull into one shared constant so a future tuning pass doesn't need to hunt for every occurrence. Plain refactor/cleanup within standing authority.
2. Investigate whether the base recurring "series" events path in `google-event-sync.service.ts` `apply()` (~line 122-127) needs the same batching treatment as standalone events and instance pages got — it still processes series events one at a time via `applyOne`/`insertOne`/`replaceOne`. Likely low-volume and fine as-is, but worth confirming against real import sizes before calling it done; fix if it's a real gap, within standing authority as a bug fix.
3. No code work available from packet 09 — the only unchecked "V1 complete" box (master-doc line 58) is the PO-gated staging rehearsal/manual-acceptance runbook, explicitly marked "agents must not perform these." Standing by for any fixes that rehearsal surfaces once PO runs it.

### Blockers
- None.

## Architect

### Recent work
- Diagnosed and repaired staging's perpetual Google sync state (backend/metadata/SSE/web), landed with regression coverage (yesterday's work, per notes/2026-07-13.md).
- Reviewed the event-contract cleanup (#2113, #2116): consolidated `LegacyEvent`/`CompassEvent` contracts, ported web grid-event schemas to zod/v4, moved web-only types out of core — held the agreed patterns with no v3/v4 collision.
- Confirmed `packages/web/tsconfig.app.json`'s removed `@backend/*` alias is a clean no-op (zero remaining `@backend/` imports in web), consistent with the contract cleanup's web/backend decoupling.

### Top 3 priorities
1. Close the staging evidence gate: run Phase S2's 12-step manual acceptance and Phase S3's rollback + second-forward-run rehearsal (the only two `pending` cells in the runbook's evidence table). This is PO-gated per the runbook and founder's 2026-07-13 DEFERRED decision. **[needs approval]**
2. Once the staging gate closes, prepare the production go/no-go checklist from `prod-cutover-plan-subcalendar-v1.md` (tag selection, backup verification, maintenance window, rollback owner) — the actual cutover execution is a destructive production action. **[needs approval]**
3. Repopulate `team/backlog/` with the next near-term packet now that v1 build work is fully archived and only the cutover rehearsal remains — architect standing authority to draft, but choosing *what* comes after v1 cutover is a priority call. **[needs approval]**

### Blockers
- None — the sole item blocking production cutover (packet 09 manual acceptance + rollback rehearsal) is PO-gated, not Architect's to unblock, and was already deferred by the founder on 2026-07-13.

## Product Owner

### Recent work
- Staging cutover for Google sub-calendars v1 advanced substantially since yesterday: validator repair, code-121 elimination, and three performance fixes (PRs #2107/#2109/#2110/#2111, release `v1.0.211`) landed, with staging-cloud and staging-selfhosted both now on the final strict schema and zero invalid rows on a designated-account full resync — this is real, logged evidence in `team/architect/google-subcalendar-big-bang-runbook.md`, not a metric I'm asserting.
- The window continued the net-deletion trend (258 files, +3526/-3872): seed CLI removed, `LegacyEvent` renamed to `CompassEvent`, event contracts/types cleaned up, `eventemitter` dependency dropped, command palette event actions unified — internal/dev-facing, no new user-visible surface removed this window beyond what was already gone (someday/priority/tasks, removed as of v1.0.194, predates this window).
- One genuinely user-visible addition landed: PR #2117 moved calendar sync status into the command palette as a quiet status line, replacing the sidebar's orange-coloring/tooltip remediation pattern — a real UX improvement to how users see sync health.

### Top 3 priorities
1. **[needs approval]** Run the two still-pending staging evidence cells — the 12-step manual acceptance runbook and the rollback/second-forward-run rehearsal (`team/architect/google-subcalendar-big-bang-runbook.md` lines 202, "Staging evidence gate") — with a real Google account. Every other cell in that gate is now filled and passing; these two are the *entire* remaining distance between "built" and "production users have multi-calendar." Founder deferred this exact ask yesterday ("not now"); re-raising because staging evidence has materially advanced since then — worth a fresh yes/no rather than carrying forward yesterday's deferral by default.
2. Confirm the sidebar sync-status simplification (#2117) didn't regress the read-only-calendar left-click-open race noted as a known follow-up in the master-doc's v1-outcome checklist — it touches the same sidebar surface QA flagged. Tracing, not new work; recommend QA/fullstack fold this into their next sidebar pass rather than a standalone task.
3. Once production cutover happens, prioritize a first-week usage read (event counts by provider, sync error rate, watch-repair frequency) as the initial real signal for this role — naming it now so it's ready to request the moment prod flips, not invented ahead of time.

### Blockers
- No usage metrics or user feedback were supplied for this standup window — per the skill, not inventing any. Concretely, once production is live I'd want: (a) Google-calendar-connect completion rate (did users who started OAuth finish import?) from backend auth/import logs, to validate the multi-calendar onboarding flow; (b) sync error rate / watch-repair frequency per user from the sync service logs, to know if the reliability hardening (packets 04-07) actually holds under real traffic; (c) any support/feedback mentioning "someday" or "tasks" post-cutover, since those features were removed with no replacement and I have no way to confirm from the repo alone whether real users miss them.

## Needs founder

1. **Staging evidence gate (Architect #1, PO #1 — same ask, both roles).** The 12-step manual acceptance and the rollback + second-forward-run rehearsal are the only two `pending` cells; production stays locked while either is pending. **These are PO-gated — "agents must not perform these" — so a yes means *you* run them, not a role session.** You deferred this on 2026-07-13; both roles re-raised it because the other four cells have since gone green on `v1.0.211`. Run the staging rehearsal now?
2. **Production go/no-go checklist (Architect #2).** Draft the prod cutover checklist (tag, backup verification, maintenance window, rollback owner) from `prod-cutover-plan-subcalendar-v1.md` — a document, not the cutover itself. Prepare it now so it's ready when the gate closes, or wait until the gate actually closes?
3. **Repopulate `team/backlog/` (Architect #3).** v1 build work is fully archived and `team/backlog/` no longer exists; choosing what comes after the v1 cutover is a priority call, not an architect default. Should the architect draft the next packet, and if so — pointed at what?
4. **Post-cutover metrics access (PO Blocker).** PO has no usage signal at all and won't invent any. Wants, once prod is live: Google-connect completion rate, sync error / watch-repair rate, and any support feedback mentioning the removed someday/tasks features. Will you wire up that access?

## Founder decisions (2026-07-14)

- DEFERRED: architect/product-owner: run the two pending staging evidence cells (12-step manual acceptance, rollback + second forward run) — founder chose to defer a second day. Production remains on the legacy collection and the gate stays open. Both roles should expect to re-raise; the deferral is not a rejection.
- APPROVED: architect: draft the production go/no-go checklist (tag selection, backup verification, maintenance window, rollback owner) from `prod-cutover-plan-subcalendar-v1.md`. Document only — the cutover itself remains locked. Rationale: cheap and reversible, so the cutover isn't waiting on paperwork once the gate closes.
- DEFERRED: architect: repopulate `team/backlog/` with a next packet — hold until production is live. Founder does not want a new front opened while the finished feature is still not in users' hands.
- APPROVED (pending cutover): product-owner: metrics/log access (Google-connect completion rate, sync error + watch-repair frequency, support feedback on removed someday/tasks) to be wired up *after* production cutover. PO should stop re-raising this as a blocker until then; it is granted, not refused.

### Standing-authority work adopted this run (no approval required)

- fullstack: dedupe the `perPage = 1000` magic number across 6 call sites in `google-event-sync.service.ts`, `google-sync.service.ts`, and `google-import.service.ts`, and assess whether the base recurring-series path in `apply()` needs the batching the standalone/instance paths received. Traces to the fullstack charter as plain cleanup + a possible perf gap; executed via the standup workflow.
- Not adopted: QA's `qa-test-staging` sweep and staging log review — deliberately left for a `/qa-test-staging` run rather than folded into this standup's merge queue.
