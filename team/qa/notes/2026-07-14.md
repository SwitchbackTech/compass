# QA notes — 2026-07-14

## Cleanup

Evening cleanup ritual over the review window `6f9448d7..96cd5e7e` (81 commits
since the last cleanup). The marker had been reset to `6f9448d7` by the
2026-07-13 workflow restructure (#2074), so the window was unusually large, but
most of it was CI-ignored `team/`/`handoff/` doc moves and feature *removals*
(tasks, someday) that are already net simplifications. The real cleanup surface
was the net-additive product code in three clusters: Day-view split-by-calendar,
the sidebar calendar list + calendar-aware CRUD/forms, and the backend Google
sub-calendar sync services.

Analysis fanned out over those three clusters (fresh-eyes cross-PR pass — each
PR was already `simplify`-ed at ship time, so the target was duplication that
emerged *between* PRs, especially the hand-reconciled #2091/#2092 merge). Only
high-confidence, behavior-preserving findings were taken; medium-confidence
structural extractions were deferred (see Founder follow-ups).

### Merged

- [#2133](https://github.com/SwitchbackTech/compass-calendar/pull/2133)
  `refactor(web): remove dead EventForm plumbing and latest-ref indirection`
  → v1.0.227. Removed two "latest-ref" mirrors (`useRef` + syncing `useEffect`)
  around already-stable view-command handlers in `DayCalendarGrid` and
  `useDayCalendarScrollToNow` — `onViewCommand` returns its own unsubscribe and
  `emitViewCommand` reads the listener set at emit time, so subscribing the
  handler directly is equivalent. Dropped the never-supplied `onEscape` prop
  from `useDayViewShortcuts` (kept the Escape registration for `blurOnTrigger`),
  and dead `EventForm` plumbing left by the sidebar-docking/someday-removal
  merge: `titleInputRef` (+ its orphan test), `descriptionRef`, the `category`
  type field, and `SetEventFormField`'s unused `value` param. Net −64 lines,
  −4 `useRef`, −2 `useEffect`, zero added state.

- [#2134](https://github.com/SwitchbackTech/compass-calendar/pull/2134)
  `refactor(backend): dedupe repeated helpers across google sync services`
  → v1.0.228. Extracted `importedGCalIds(sync)` (the identical nextSyncToken
  set built in both `runReconcile` and `reconcileFromFullList`),
  `tearDownEventMachinery()` (the stopWatch + removeSyncEntry + delete trio
  shared by full removal and the freeBusyReader downgrade), and
  `assertGoogleCalendar()` (the `provider !== "google"` guard repeated at all
  three import entry points, now one type-narrowing assertion).

Both PRs: correctness-reviewed on the diff (no findings), type-check green, unit
suites green, CI green, squash-merged, deployed to staging with health checks
passing.

### qa-ux-sweep (step 4)

Ran an anon "Local" session against a local dev build (`localhost:9084`).
Exercisable surfaces are clean: named regions, labeled event buttons, proper
month-picker listbox/option semantics, working focus management, and zero
console errors. Opportunistically confirmed #2133's behavior live — clicking an
event opens the docked form with the title input auto-focused, Escape closes it
and blurs focus, and the `c` shortcut opens a new timed draft (the exact
view-command subscription the PR rewrote). **No fixable UX/a11y friction found →
no step-4 fix PR.** The window's headline multi-calendar surfaces (visibility
controls, split across several calendars, calendar identity on cards, read-only
calendars, command-palette sync status) can't be exercised in a single-calendar
anon session and are left for the founder's staging review.

### Founder follow-ups

- **`qa-test-staging` not run.** It requires the founder's real Chrome unlocked
  with the extension connected in the staging profile; that precondition isn't
  met in this autonomous run, so the staging sweep is left for the founder
  rather than reporting a green run that wasn't done.
- **Deferred medium-confidence extractions** (higher value, but each carries a
  behavior-change risk an auto-merge-on-green ritual shouldn't take blind):
  - `google-calendarlist.service`: dedupe the shared back half of `runReconcile`
    / `reconcileFromFullList` (removals → upsert-apply → token-advance → publish).
    Hinges on the "advance the sync token only after every step" ordering
    invariant that unit tests may not fully pin.
  - `google-watch.service`: extract the shared channel-create-and-persist middle
    of `startCalendarListWatch` / `startEventWatch` (differing error branches
    make it a partial extraction).
  - `google-event-sync.service`: a `reconcileByProviderId` helper for the
    adopt-existing-identity-then-bulkReplace shape in `applyStandalone` /
    `replaceOccurrences` / `applyOne` (externalReference null-handling differs
    slightly between sites).
  - Day view: dedupe interaction wiring across `DayAllDayCalendarEvent` /
    `DayTimedCalendarEvent` and the two `DayCalendarEventLayers` (layers
    legitimately diverge on layout, so a judgment call).
  - Cross-view: the near-identical "focus sidebar" helper in `DayViewContent`
    vs `useWeekShortcuts`.
- **Latent coupling, not a bug** (out of cleanup scope — `/code-review`
  territory if pursued): the Week sidebar event-details panel reads "is the form
  open" from two sources — the draft *store* (`WeekView`) and the draft
  *context* (`WeekSidebarEventDetails`). They stay in sync today.

### Token spend

Logged at hand-off (see founder close-out).
